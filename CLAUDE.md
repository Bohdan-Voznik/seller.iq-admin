@AGENTS.md

## TODO

- **Страница «Операции» (`src/app/(protected)/operations/page.tsx`) временно скрыта из навигации** (2026-08-23, ссылка убрана из `src/app/(protected)/layout.tsx`, роут остался доступен напрямую). Нужно доработать позже:
  - привести к тому же стилю действий, что и на странице юзера (`src/components/users/user-actions.tsx`) — `ConfirmActionButton` везде, единая ширина кнопок и т.д.
  - «Синхронизировать кабинет» — заменить сырой числовой `userId`/`cabinetId` на выбор пользователя + `Select` кабинета по названию (как уже сделано в `UserActions`), а не голые ID.
  - «Экспорт заказов кабинетов» — уточнить, актуален ли ещё эндпоинт `/admin/rztk-cabinet/orders-export` (даты без времени, не проверено).
  - решить, нужна ли эта страница вообще отдельно от per-user «Действия», или часть операций (scan-all, categories/scan-all) остаётся глобальной, а остальное убрать как дубли.
